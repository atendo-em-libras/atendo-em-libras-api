CREATE TABLE hospital_clinic_attendances (
    id serial primary key,
    state varchar,
    state_initials varchar,
    city varchar,
    street_name varchar,
    street_number varchar,
    complement_info varchar,
    name varchar,
    phone varchar,
    email varchar
);